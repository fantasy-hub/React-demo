import React, { Component } from 'react'
import Base from './Base'

export default class index extends Component {
    render() {
        return (
            <div>
                <Base stage="React"></Base>
            </div>
        )
    }
}
